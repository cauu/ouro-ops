# S0017 p5-12: operation-scoped spec — only the always-required fields. Registration-only
# fields (ticker/metadata/economics), node_version, sync and upgrade are omitted; the
# operations that consume them fail closed with an actionable message. This is the shape the
# onboarding generator emits for non-deploy operations (e.g. observability, kes-rotation).
spec_version: 1
pool:
  network: preprod
  network_magic: 1
  genesis_hashes:
    shelley: "162d29c4e1cf6b8a84f2d692e67a3ac6bc7851bc3e6e4afe64d15778bed8bd86"
topology_mode: p2p
machines:
  - id: bp1
    role: bp
    ssh:
      host: 10.0.0.10
      port: 22
      user: ouro-exec
      key_ref: "creds://bp1"
  - id: relay1
    role: relay
    public_endpoint:
      host: relay1.example.com
      port: 3001
    ssh:
      host: 203.0.113.5
      port: 22
      user: ouro-exec
      key_ref: "creds://relay1"
