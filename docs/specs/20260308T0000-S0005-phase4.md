# Cardano Stake Pool 控制平面 Phase 4 活动规范

Spec-ID：`S0005`
状态：`active`
创建时间：`2026-03-06`
开始时间：`2026-03-08T0000`
完成时间：
前一个 Spec-ID：`S0004`
结项原因：

## 1. 需求详情

- 背景
  - Phase 1 至 Phase 3 的基础能力已经完成，并已单独归档。
  - 当前活动范围是 Phase 4 及跨阶段质量项，用于补齐监控、KES、升级、回退和错误处理，使产品达到更完整的可用状态。
- 范围
  - 监控快照与轮询。
  - KES 状态、生成、导入与推送流程。
  - 滚动升级、门控确认与回退。
  - Dashboard、KES、升级相关前端体验。
  - 错误处理与审计覆盖等跨阶段能力。
- 约束
  - 继续沿用既有 local-first 架构：Tauri + Rust + SQLite + Python sidecar + Ansible。
  - 保持 Phase 3 已交付的部署基线，包括 blinklabs 默认镜像、takeover 与 safe validation mode。
  - 关联关系继续由应用层负责，不引入 SQLite 外键与级联。
  - 同一时间只能有一个活动 spec。
- 非目标
  - 不在本 spec 中重新打开已归档的 Phase 1 至 Phase 3 范围。
  - 不改写已有历史参考文档。

## 2. 概要设计

- 架构 / 受影响模块
  - 后端 commands：`monitor`、`kes`、`upgrade`。
  - 前端页面与 store：`Dashboard`、`KesManager`、`UpgradeWizard`、监控状态管理。
  - Ansible playbook：`upgrade.yml`、`rollback.yml`、`kes-push.yml`。
- 数据模型与接口
  - `machine_health` 作为周期性监控快照的主要落库位置。
  - `kes_state` 作为 KES 状态与过期信息的主来源。
  - 升级流程在任务状态机中加入 relay 逐台推进、BP 门控与回退元数据。
- 风险与回退策略
  - 升级与 KES 流程中，所有影响 BP 的动作都必须经过显式确认。
  - 回退仍然采用前向变更模式，依赖 rollback playbook 与验证证据。

## 3. 执行计划

- [x] `p4-1` 实现 `commands/monitor.rs`：`monitor_snapshot`、`monitor_start_polling`、`monitor_stop_polling`
- [x] `p4-2` 实现指标采集与 `healthy`、`warning`、`critical` 阈值映射
- [x] `p4-3` 实现前端监控 store 与 Dashboard 数据流
- [x] `p4-4` 实现 `kes_status_all`、`kes_generate`、`kes_import_cert`
- [x] `p4-5` 增加 `ansible/playbooks/kes-push.yml`
- [x] `p4-6` 实现 `upgrade_start`、`upgrade_confirm_next`、`upgrade_rollback`
- [x] `p4-7` 增加 `ansible/playbooks/upgrade.yml` 与 `rollback.yml`
- [x] `p4-8` 实现滚动升级状态机与 `upgrade:gate`
- [ ] `p4-9` 实现 `KesManager` 页面与轮换流程
- [ ] `p4-10` 实现 `UpgradeWizard` 页面与门控处理
- [ ] `p4-11` 实现 Dashboard 卡片、KES 倒计时与最近任务视图
- [ ] `p4-12` 为 BP 升级与 KES 推送补齐 ticker 确认的高危确认能力
- [ ] `p4-13` 运行并记录完整的 Phase 4 验证
- [x] `p4-14` 整理已完成 Phase 3.6 spec 的文件名与归档入口
- [x] `p4-15` 按新 immutable-spec 规则迁移 spec 目录结构、文件名和头部元数据
- [x] `p4-16` 将运行时配置更新与容器重启能力纳入 Phase 4 方案并明确实现边界
- [x] `p4-17` 实现运行时配置更新能力：生成角色化 `config.json`、下发到目标机并触发受控重启
- [x] `p4-18` 实现运行时容器重启能力：为单机节点提供独立的 restart 操作，不复用 deploy 语义
- [x] `p4-19` 在前端增加运行时配置应用与容器重启入口，并展示操作状态与风险提示
- [x] `p4-20` 清理与当前事项无关的工作区残留格式化改动与构建产物
- [ ] `x-1` 统一前端对 `AppError` 的处理与用户可读错误展示
- [ ] `x-2` 确保所有关键操作写入 `audit_log`

## 4. 测试与验收标准

- `TC-P4-001` 监控 command 与 Dashboard 流程覆盖快照、轮询、持久化和阈值判定。
- `TC-P4-002` KES 流程覆盖状态查询、密钥生成、证书导入校验与推送到 BP。
- `TC-P4-003` 升级流程覆盖 relay 逐台推进、BP 确认门控、回退参数与状态转换。
- `TC-P4-004` 前端流程覆盖 Dashboard 卡片、升级门控确认与 KES 工作流页面。
- `TC-P4-005` 安全确认与回退集成覆盖 dangerous 确认、ticker 确认与 rollback inventory 使用。
- `TC-P4-006` 已完成 spec 的文件名、状态与 `docs/README.md` 入口应一致；Phase 3.6 归档文件应使用 completed 形态文件名，且活动入口保持指向 Phase 4。
- `TC-P4-007` spec 目录结构应迁移为：`docs/specs/` 根目录仅保留当前 active spec，`docs/specs/draft/` 用于未开始 spec，`docs/specs/completed/` 用于已结项 spec；所有 spec 头部应包含 `Spec-ID`、状态、创建/开始/完成时间、前一个 Spec-ID 与结项原因字段。
- `TC-P4-008` 运行时配置更新流程覆盖 relay/bp 角色化 `config.json` 渲染、远端文件下发、受控重启与重新读取配置后的生效验证。
- `TC-P4-009` 运行时重启流程覆盖单机重启命令、审计记录、前端状态展示与“无需经过 deploy/mithril 链路”的语义分离。

## 5. 执行日志（仅追加）

- `2026-03-06` 从 `docs/development-plan/v1.0.md` 与 `docs/detail-design/v1.0.md` 中提取剩余范围，初始化本 Phase 4 活动 spec。
- `2026-03-06` 当前尚未在本 spec 下启动任何实现项。
- `2026-03-08` `p4-14` 完成：将已完成的 Phase 3.6 归档 spec 文件名从 `2026-03-07-phase3-6-mithril-bootstrap-active.md` 调整为 completed 形态，并同步更新 `docs/README.md` 与相关入口引用。
- `2026-03-08` `p4-15` 完成：按新版 immutable-spec 规则重排 spec 目录；为历史 spec 回填 `Spec-ID` 和元数据头部，将已结项 spec 移入 `docs/specs/completed/`，将当前 active spec 重命名为开始时间前缀形态，并创建 `docs/specs/draft/` 目录占位。
- `2026-03-08` `p4-1` 完成：为监控链路补齐 `monitor_start_polling` 与 `monitor_stop_polling` 两个 Tauri command；新增应用内单例 polling 句柄，通过 `monitor:snapshot` / `monitor:error` 事件向前端推送采样结果，并保留 `monitor_snapshot` 作为一次性拉取接口。
- `2026-03-08` `p4-2` 完成：为 `MonitorSnapshot` 增加 `health_level` 字段，并将监控结果映射为 `healthy`、`warning`、`critical` 三档；规则覆盖已同步、正常追块、恢复中、超时、不可达与同步停滞等状态。
- `2026-03-08` `p4-3` 完成：新增 `src/lib/monitorStore.ts` 统一管理监控快照、后台轮询和事件订阅；Dashboard 改为通过 store 消费 `monitor:snapshot` / `monitor:error` 事件流，并展示 `health_level`，不再在页面内维护独立的 `setInterval` 轮询。
- `2026-03-09` `p4-4` 完成：新增 `src-tauri/src/commands/kes.rs`，实现 `kes_status_all`、`kes_generate`、`kes_import_cert` 三个后端命令；其中 `kes_generate` 生成新的 KES 密钥并返回离线签名指引，`kes_import_cert` 校验证书类型并创建 `kes_rotation` 待执行任务，真正推送到 BP 的 playbook 仍留在 `p4-5`。
- `2026-03-09` `p4-5` 完成：新增 `ansible/playbooks/kes-push.yml` 与 `ansible/roles/cardano-node/tasks/kes_push.yml`，并在后端补齐 `kes_push_start`、`kes_rotation_status` 两个命令；`kes_import_cert` 产生的 `kes_rotation` 任务现在可真正执行 BP 证书安装、容器重启和 readiness 检查。
- `2026-03-09` `p4-16` 完成：根据运行中的 relay/bp 行为反馈，明确将“运行时配置更新”和“运行时容器重启”纳入 Phase 4；约束其与 deploy 语义分离，避免配置生效仅依赖完整重部署链路。
- `2026-03-09` `p4-17` 完成：新增 `runtime_apply_config` / `runtime_config_status` 后端命令与 `ansible/playbooks/runtime-config.yml`；mainnet `config.json` 改为按角色渲染，relay 使用更健康的 steady-state peer targets，bp 的 peer targets 则按 relay 数量动态收敛；运行时配置应用链路仅更新配置并对现有容器执行受控重启，不复用 deploy/mithril 语义。
- `2026-03-09` `p4-18` 完成：新增 `runtime_restart` / `runtime_restart_status` 后端命令与 `ansible/playbooks/runtime-restart.yml`；该链路只对单机现有 `cardano-node` 容器执行 restart 和 readiness 检查，不复用 deploy、takeover 或 mithril 冷启动逻辑。
- `2026-03-09` `p4-19` 完成：在 `MachineManager` 中为 relay/bp 增加 `Apply Runtime Config` 与 `Restart Runtime` 两个入口；前端会轮询对应 task 状态，并展示“与 deploy / mithril 路径分离”的风险提示与错误信息。
- `2026-03-09` `p4-6` 完成：新增 `src-tauri/src/commands/upgrade.rs`，实现 `upgrade_start`、`upgrade_confirm_next`、`upgrade_rollback` 三个后端命令；`upgrade_start` 会按 relay 优先于 bp 的顺序创建升级任务并持久化机器计划，`upgrade_confirm_next` 仅允许从 `AWAIT_NEXT_RELAY` / `AWAIT_BP_CONFIRM` 两个门控阶段推进，`upgrade_rollback` 会为指定机器创建独立的 `upgrade_rollback` 任务并保留 `previous_version` 与 `backup_archive` 元数据。
- `2026-03-09` `p4-7` 完成：新增 `ansible/playbooks/upgrade.yml` 与 `ansible/playbooks/rollback.yml`，并补齐 `ansible/roles/cardano-node/tasks/upgrade.yml` / `rollback.yml`；升级 playbook 支持 `BACKUP_CONFIG`、relay `serial: 1` 升级段和 bp 升级段，回滚 playbook 支持使用 `backup_archive` 与 `previous_version` 恢复配置和旧镜像。
- `2026-03-09` `p4-8` 完成：`upgrade_start` 现在会启动真实的后台 worker，串行执行 `BACKUP_CONFIG -> UPGRADING_RELAY_N -> HEALTH_GATE_RELAY -> AWAIT_NEXT_RELAY / AWAIT_BP_CONFIRM -> UPGRADING_BP -> HEALTH_GATE_BP -> SUCCESS`；在每个需要用户放行的节点发出 `upgrade:gate` 事件，`upgrade_confirm_next` 会将任务从 gate 状态恢复到下一阶段，`upgrade_status` 则提供前端轮询入口。
- `2026-03-09` `p4-20` 完成：清理 `deploy.rs`、`kes.rs`、`monitor.rs`、`runtime.rs` 中与当前事项无关的纯格式化残留，以及 `tsconfig.tsbuildinfo` 构建产物，避免后续 commit 混入无关工作区噪音。

## 6. 验证证据（仅追加）

- `2026-03-06` 暂无验证证据。后续会在 Phase 4 各事项实现并验证后逐条追加。
- `2026-03-08` `TC-P4-006 | stack: other | command: manual inspection of docs/specs and docs/README.md | result: pass | note: Phase 3.6 归档文件已重命名为 completed 形态，README 当前活动入口仍为 Phase 4，归档入口已更新`
- `2026-03-08` `TC-P4-007 | stack: other | command: manual inspection of docs/specs tree, spec headers, and docs/README.md | result: pass | note: active spec 已独占根目录，completed spec 已进入 completed/，draft 目录已创建，所有现存 spec 头部元数据与文件名规则已统一`
- `2026-03-08` `TC-P4-001 | stack: rust | command: cargo test -q | result: pass | note: tc_mon_013 与 tc_fe_007 覆盖 monitor polling interval 约束、IPC 暴露与 invoke handler；monitor_start_polling / monitor_stop_polling 已纳入后端命令集合`
- `2026-03-08` `TC-P4-001 | stack: node | command: pnpm build | result: pass | note: 新增 monitor polling IPC 封装后前端仍可正常构建`
- `2026-03-08` `TC-P4-001 | stack: rust | command: cargo test -q | result: pass | note: tc_mon_014/tc_mon_015/tc_mon_016 覆盖健康等级阈值映射；已同步与高速追块归类为 healthy，恢复中与低速追块归类为 warning，不可达与停滞归类为 critical`
- `2026-03-08` `TC-P4-001 | stack: node | command: pnpm build | result: pass | note: MonitorSnapshot 新增 health_level 字段后前端类型和构建保持一致`
- `2026-03-08` `TC-P4-001 | stack: rust | command: cargo test -q | result: pass | note: tc_fe_006/tc_fe_008 覆盖 Dashboard 通过 monitorStore 消费 polling 事件、启动/停止后台轮询并展示 health_level`
- `2026-03-08` `TC-P4-001 | stack: node | command: pnpm build | result: pass | note: monitorStore、Dashboard 事件订阅和手动 refresh 新数据流均可正常构建`
- `2026-03-09` `TC-P4-002 | stack: rust | command: cargo test -q | result: pass | note: tc_kes_001~tc_kes_004 覆盖 KES 状态严重级别映射、生成签名请求、证书类型校验与 BP 行级状态读取；tc_fe_009/tc_fe_010 覆盖 IPC 暴露与前端类型定义`
- `2026-03-09` `TC-P4-002 | stack: node | command: pnpm build | result: pass | note: 新增 kesStatusAll/kesGenerate/kesImportCert IPC 与 KesStatus/KesSignRequest 类型后前端仍可正常构建`
- `2026-03-09` `TC-P4-002 | stack: rust+node+ansible | command: cargo test -q && pnpm build && ANSIBLE_ROLES_PATH=ansible/roles ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check ansible/playbooks/kes-push.yml | result: pass | note: KES push playbook、运行时执行链路与 IPC 已闭合；tc_kes_005、tc_dep_016 与 tc_fe_012 覆盖 task 状态回读、playbook 静态存在性和前端接口暴露。`
- `2026-03-09` `TC-P4-008 | stack: other | command: manual spec review | result: pass | note: 已将角色化 config 更新、受控重启与独立 runtime restart 操作纳入 Phase 4 计划与验收标准，边界上明确与 deploy/mithril 链路分离`
- `2026-03-09` `TC-P4-008 | stack: rust | command: cargo test -q | result: pass | note: tc_cfg_001~tc_cfg_003 覆盖 runtime config inventory 构造、非 relay/bp 机器拒绝与 runtime_config 任务状态回读；tc_dep_013/tc_dep_014 覆盖 role-aware mainnet config 模板与 runtime-config playbook 的静态存在性`
- `2026-03-09` `TC-P4-004 | stack: other | command: git diff -- src-tauri/src/commands/deploy.rs src-tauri/src/commands/kes.rs src-tauri/src/commands/monitor.rs src-tauri/src/commands/runtime.rs tsconfig.tsbuildinfo && git restore -- src-tauri/src/commands/deploy.rs src-tauri/src/commands/kes.rs src-tauri/src/commands/monitor.rs src-tauri/src/commands/runtime.rs tsconfig.tsbuildinfo && git diff --exit-code -- src-tauri/src/commands/deploy.rs src-tauri/src/commands/kes.rs src-tauri/src/commands/monitor.rs src-tauri/src/commands/runtime.rs tsconfig.tsbuildinfo | result: pass | note: 已确认这些文件仅包含无关格式化残留或构建产物，并已从工作区清理，不影响当前 Phase 4 功能链路。`
- `2026-03-09` `TC-P4-009 | stack: rust+node+ansible | command: cargo test -q && pnpm build && ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check ansible/playbooks/runtime-restart.yml | result: pass | note: 运行时重启链路已覆盖单机 task 落库、状态查询、独立 playbook 与“缺容器时改用 deploy”的语义分离。`
- `2026-03-09` `TC-P4-004 | stack: rust+node | command: cargo test -q && pnpm build | result: pass | note: MachineManager 已增加运行时配置应用与容器重启入口，前端可展示 task 状态、错误信息与“无需经过 deploy/mithril 链路”的风险提示。`
- `2026-03-09` `TC-P4-003 | stack: rust+node | command: cargo test -q && pnpm build | result: pass | note: tc_upg_001~tc_upg_004 覆盖 relay 优先排序、至少一台 relay 的门控、确认阶段限制与 rollback 元数据透传；tc_upg_005、tc_fe_013 与前端 IPC 封装覆盖 upgrade 命令注册与调用入口。`
- `2026-03-09` `TC-P4-003 | stack: rust+ansible | command: cargo test -q && ANSIBLE_ROLES_PATH=ansible/roles ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check ansible/playbooks/upgrade.yml && ANSIBLE_ROLES_PATH=ansible/roles ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check ansible/playbooks/rollback.yml | result: pass | note: tc_dep_017 覆盖 upgrade/rollback playbook 静态存在性、relay 串行升级配置与 backup_archive/previous_version 接口；两份 playbook 已通过 syntax check。`
- `2026-03-09` `TC-P4-003 | stack: rust+node+ansible | command: cargo test -q && pnpm build && ANSIBLE_ROLES_PATH=ansible/roles ANSIBLE_LOCAL_TEMP=/tmp/ansible-local ansible-playbook --syntax-check ansible/playbooks/upgrade.yml | result: pass | note: p4-8 已补齐 upgrade worker、gate 事件与 upgrade_status；tc_upg_005/tc_upg_006 与 tc_evt_004 覆盖 gate payload、状态查询与命令/前端暴露。`

## 7. 变更记录（仅追加）

- `2026-03-06` 当前活动范围已从早期文档收敛记录中拆分出来，后续仅追踪已归档 Phase 1 至 Phase 3 之后的前向工作。
- `2026-03-06` 因新增“部署成功判定解耦与同步速度监控”中间范围，当前文档降为 `draft`。新的执行入口为 `docs/specs/completed/20260306T0020-S0003-phase3-5-deploy-readiness-and-sync-monitoring.md`。
- `2026-03-08` 用户确认 Phase 3.6 结项后，本 spec 恢复为唯一活动 spec，继续承接 Phase 4 与跨阶段质量项。
