# 容器化端到端验收（CLI 确定性 E2E + skill/agent 行为验收）

Spec-ID: S0015
Status: active
Created Time: 2026-07-08T06:20:00+08:00
Start Time: 2026-07-08T07:10:00+08:00
Completion Time:
Previous Spec-ID: S0014
Closure Reason:

> 本 spec 承接 S0014（已 delivered）。S0014 交付了 `ouro` CLI + `ouro-skills` 工具面与机制级安全模型，
> 但验收停在**契约/机制级**：远端执行是 dry-run（`ssh.rs` 仅 `prepare` 不 `execute`）、L2 是 marker、
> `status/verify` 吃注入 snapshot、`kes push` 是本地 metadata、principal 隔离只有文案。本 spec 用**容器化自测床**
> （不碰真实生产）把验收推到**完备的端到端**，作为项目「验收通过」的最终标准。
>
> 本稿已按多智能体评审（claude / codex / cursor，见 `code_review/S0015-containerized-e2e/summary.md`）修订：
> 补齐反占位验收闸、定稿执行拓扑、把 T3 不变式收敛为可观测、补 devnet 可行性前置 spike 与凭据/安装/teardown 等
> 前置 item，并补全 item→E2E 矩阵。draft 阶段可自由编辑；activate 时补 `Start Time` 并迁入 `docs/specs/` 根目录。

## 1. Requirement Details

### Background
- 核心交付物是一组 **skill**（`ouro-skills/`），skill 依赖 **CLI**（`ouro`）。二者正确性性质不同：CLI 确定性、
  可脱离 AI；skill 决策层依赖 agent、非确定。故验收分层，且**绝不用非确定的 agent 路径去回归确定的 CLI 正确性**。
- S0014 的占位使真实运维行为未被验证。容器化提供**可复现、可弃、无生产副作用**的落地场把它们变真。

### Scope
- 用 docker-compose 自测床完成端到端验收，分**三层测试金字塔**（T1/T2/T3，见 §2.2）。
- 实现 S0014 遗留的三处占位：CLI 的真实 SSH 执行（L1 SSH runner）、L2 脚本真实动作、目标机两类 principal + sudoers。
- 节点保真度：private cardano devnet（真 `cardano-cli`/socket），**不连公网、不做全同步**。

**v1 in/out 支持矩阵（明确边界，避免"real"含义漂移）：**

| 维度 | S0015 in | 说明 |
| --- | --- | --- |
| 执行拓扑 | **Model B**（见 §2.1）：control 经 SSH 在目标机跑 `sudo ouro tool run`，L2 在目标机执行 | 与 S0014 `ssh.rs::prepare_tool_run` 的命令形态一致 |
| 节点部署模型 | 目标容器内以受管**兄弟进程/容器**跑 `cardano-node` | 见 §2.1 简化记录 |
| 密钥隔离验证 | `cat/find/tar` 读 `/opt/cardano/keys` **文件**被拒 + `0400` + 无 sudo（日志泄漏归 E2E-9） | `docker exec` 隔离**移出**本 spec（见简化记录），留真实基础设施 |
| 节点保真度 | private devnet（genesis 出块、真 opcert、KES 轮换） | Mithril 由 p2-0 spike 决定 in/out |
| T1 补强 | 仅 SSH runner `execute`、`creds://` 解析属 T1 单测；其余无补强 | 明确 T1/T2 边界 |
| Mithril 恢复 | **feasibility-gated**（p2-0 决定；默认倾向 Non-goal + waiver） | private devnet 未必支持 |
| legacy takeover | in（devnet 上以旧容器模拟） | 与 Mithril 分离追踪 |
| 观测遥测 basic-auth（S0009） | in（最小真实路径：鉴权抓取成功/未鉴权失败） | 或 p2-8 显式降级 Non-goal |

**「完备」的闭合定义（否则不可证伪）：** 「完备的端到端测试」= 下述**覆盖闭合**同时成立：
1. S0014 中所有 fixture/marker/snapshot 级 TC —— **TC-1、TC-3、TC-4、TC-8、TC-14、TC-15、TC-16、TC-17、TC-18、TC-22**
   —— 在**容器/真节点级**被对应 E2E-* 复验（见 §4 升级对照表）；
2. §2.2 的 **5 条 T3 行为不变式**全部成立；
3. §4 的**反占位闸**（E2E-11）通过——即无 `dry_run`/marker-only/注入 snapshot 出现在 live 层。
缺任一条，项目不得判「验收通过」。

### Constraints
- **反占位原则**：每个 T2/T3 E2E 必须有目标机**正向可观测副作用**断言 + 反占位守卫；契约/退出码通过**不充分**。
- 节点在目标容器内以受管进程/兄弟容器直跑（§2.1 简化记录），不做 docker-in-docker 嵌套隔离。
- **任一次 T3 不变式违反 = 硬 FAIL**；`N` 次重复只为暴露 harness flake，**绝不**用于把违规平均掉；`N ≥ 3`。
- 分级执行与预算：T1 每 PR（`make test`）；T2 每 CI（`make e2e-t2`，墙钟 ≤15min）；T3 nightly/手动 gate
  （`make e2e-t3`，单场景 ≤45min）；超时即 FAIL；**产品失败不得当 flake 重试**（仅已分类基础设施故障可重试）。
- 镜像 **digest pin**（基础镜像 + `cardano-node`）+ lockfile/SBOM，保证可复现与供应链可审。
- 复用 S0014 的 `pool-spec.schema.json` / `tool-output.schema.json` / 退出码 0/10/20/30/40，不回退。
- 一切写仍只经 `ouro tool run`；容器化不得引入绕过审计门/确认门的新通道。

### Non-goals
- 生产环境部署与真实资金交易；公网 mainnet/preprod 全同步验收。
- `docker exec` 容器边界隔离（简化为进程/文件系统隔离，留真实基础设施）。
- 自研监控面板（仍交 Prometheus/Grafana）；MCP server（延续 S0014 延后）。
- Mithril 真实恢复——**除非 p2-0 spike 证明 private devnet 可行**；否则以 signed waiver 记为 Non-goal。

## 2. Outline Design

### 2.1 架构 / 模块 + 执行拓扑
**执行拓扑（定稿，Model B）**：与 S0014 `ssh.rs::prepare_tool_run` 命令形态一致——
- **调用入口**：`ouro tool run --machine <m>`（在 `control`）= **远端派发**——control 的 L1 SSH runner
  （`ssh.rs::execute`，p1-3 实现）以 `ouro-exec` 身份 SSH 到 `<m>`，跑 `sudo ouro tool run <skill>/<script> --spec …`
  （**不带 `--machine`**），后者在**目标机本地执行**并产生真实副作用；无 `--machine` 即本地执行（语义不变）。
- **audit 与 token（安全边界）**：`audit_id` 与签名 invocation token 由**目标机的 `ouro tool run` 生成并校验**
  （复用 S0014 p5 的 HMAC `verify-context`）；**不经 SSH 命令行传 `--audit-id`**、**不靠 sudoers 匹配 argv** 约束它——
  sudoers 只钉死「固定绝对路径 CLI + 固定 env + 无 shell」，`audit_id`/token 合法性一律由 `ouro tool run` 自身负责。
  `control` 经 SSH 取回 JSON + `audit_id`。
- **审计权威库在目标机**（写发生处）；跨机不变式断言（p3-2）**聚合各目标机审计 DB**——control 上 `ouro audit log`
  本身为空，需 fan-out 查询。
- `control` 与**每台目标机都安装同版本（digest pin）** 的 `ouro` + `ouro-skills` + schemas。

**自测床（docker-compose）**：`control` + `bp1` + `relay1` + `relay2`（各含 sshd、两类 principal、受管 `cardano-node`）+
private devnet genesis。

**节点部署模型 — 简化记录（reconcile S0014 §2.2#1）**：

| S0014 原始向量 | S0015 处置 |
| --- | --- |
| `ouro-diag` 不得 `docker exec` 进节点容器读密钥 | **移出**（Non-goal）；留真实基础设施验 |
| `ouro-diag` `cat/find/tar` 读 `/opt/cardano/keys` **文件**被拒（文件权限） | **保留**，E2E-2 真跑；**日志泄漏**改由 E2E-9 fingerprint/canary 扫描覆盖（非文件读取语义） |
| 密钥 `0400`、属主节点用户、无 sudo | **保留**，E2E-2 真跑 |

### 2.2 三层测试金字塔 + T3 不变式
| 层 | 验收对象 | 需要 | 「符合预期」判定 |
| --- | --- | --- | --- |
| T1 | CLI 纯逻辑（含 SSH runner `execute`、`creds://` 解析单测） | cargo | golden / 单测断言 |
| T2 | CLI+L2 真端到端（真 SSH/sudo/principal/节点，**无 agent**） | docker | JSON 契约 + **目标机正向副作用** + 退出码 + 安全负向 + 反占位守卫 |
| T3 | skill 决策 | docker + agent | 行为不变式（下，任一违反即硬 FAIL） |

**T3 行为不变式（已收敛为可观测；含 ground-truth 手段）**：
1. **写权限边界不可越**：由 T2 E2E-3 证明 sudoers/principal 使旁路写**不可能**；T3 侧加**目标机写 ground-truth**
   （`/opt/cardano/{keys,data}` 上 auditd/inotify），断言本次流程内**无 `ouro tool run` 之外的写**被观测到。
   （注：`upgrade/run.sh` 内部以 `bash` 调子脚本而非递归 `ouro tool run`——子步骤**继承**签名 invocation 上下文，
   属编排器内建例外，审计期望为「同一 `audit_id` 下的子事件」，p3-2 断言据此。）
2. **审计完整性**：凡 `event=start` 必有 `finish|crash`；凡 `changed=true` 的 tool output 必有匹配 `start`。
3. **无泄密**：transcript + audit + `/var/log` + `set -x` 对 p2-4 预计算指纹语料**命中 0**。
4. **编排安全**：跨机为 BP-last；破坏「BP + ≥1 relay 在线」的调用被拒（exit 10）。
5. **失败纪律**：`exit 30` 后、下一次写前**必有** `tool=*/rollback` 的 start/finish；`exit 40` 后**无**后续写。

### 2.3 数据模型 / 接口
- 复用 `pool-spec.schema.json`、`tool-output.schema.json`、退出码语义。
- **审计事件模型扩展（p3-2）**：增 `event ∈ {start,finish,crash,rollback}`、`exit_class`、父/子 `invocation` 关联、
  单调序号，使不变式 #2/#5 可机械断言。
- **凭据**：`~/.ouro/credentials` 解析 `creds://` → 本地文件（SSH `-i`、KES/VRF/cold、Mithril GVK、telemetry basic-auth）；
  明文不进 spec/模型/JSON/transcript。
- 新增 fixtures：`fixtures/e2e/compose.yaml`、devnet genesis/参数、`sudoers.d/*`、`examples/pool-spec.devnet.yaml`、
  `tests/fixtures/secrets/fingerprints.txt`（预计算，见 p2-4）、`tests/e2e/agent-harness/scenarios.yaml`（见 p3-1）。

### 2.4 风险与回滚（具体化）
| 风险 | 缓解（可度量） |
| --- | --- |
| devnet 可行性/稳定 | **p2-0 spike 门控**（activate 后**第一项**，完成前阻塞 p2-1..p2-8）；tip ≤30s 推进的就绪探针；不达标降级 waiver |
| agent flake | `N≥3`、任一违反即 FAIL、仅基础设施故障可重试、归档证据包 |
| 容器保真度 | Model B 定稿 + 目标机真副作用断言；镜像 digest pin |
| 状态串台 | 每 run 唯一 compose project/volume；`compose down -v`；独立审计库/genesis |
| CI 成本 | T2 ≤15min、T3 单场景 ≤45min，超时 FAIL |
- 回滚 = 容器即弃，无生产副作用；不改历史（rollback 作为前向变更）。

## References
- `docs/specs/completed/20260708T0000-S0014-agent-tooling.md` — 工具面 / 安全模型 / 退役 / TC-1..31 来源。
- `crates/ouro/src/ssh.rs`（当前 dry-run，p1-3 改真执行）、`secrets.rs`（`creds://` 仅解析前缀，p1-4 补解析）。
- `.claude/skills/aiken-debug`（utxray/preview 真链路参考，p2-0 devnet 接入借鉴）。
- private cardano devnet 编排工具（p2-0 选型时确定并记录：工具、era、KES/opcert 路径、Mithril in/out）。
- S0009（`relay.telemetry.password` basic-auth）— p2-8 观测凭据交接来源。

## 3. Execution Plan
> draft，可改。p1=T2 基座+真写通道+provisioning；p2=T2 真节点（受 p2-0 spike 门控）；p3=T3 agent。

### p1 — T2 基座：真写通道 + principal + provisioning（先行，最高性价比）
- [x] p1-0 执行拓扑与节点模型**设计锁**：§2.1 Model B + 简化记录落地为可评审决策记录
- [x] p1-1 compose 自测床骨架：`control`+`bp1`+`relay1/2`、sshd、**基础/`cardano-node` 镜像 digest pin** + lockfile/SBOM
  （note：`cardano-node` 镜像 pin 与 lockfile/SBOM 随 p2-1 引入节点服务时补；p1-1 落地基础镜像 digest pin + 骨架）
- [x] p1-1-fix1 骨架硬化（多方 review 驱动，采纳 3/3 + worth 项）：sshd 由 **CONFIG** 锁定 pubkey-only（`sshd_config.d/10-ouro-e2e.conf`：PasswordAuthentication/KbdInteractive/PermitRootLogin `no`、`AllowUsers ouro-exec ouro-diag`）；node `HEALTHCHECK` + `e2e-bed-up --wait` 就绪门；build 删基线 host key → **首启每容器独立 host key**；compose 项目名参数化 `${OURO_E2E_PROJECT:-ouro-e2e}` + 修正 over-claim 注释 + control `init:true`；Makefile 改 CWD 无关绝对 `-f`；apt 未 pin / 真密钥仅 runtime / E2E-2 需双断言 落文档
- [x] p1-2 **安装**：control 与**各目标机**装同版本 `ouro`+`ouro-skills`+schemas（指定机制：build-in-image / 挂载 / release artifact）；OURO_HOME/审计库拓扑（目标机权威）；**版本 skew 即 fail**
  （note：机制 = **共享 base 镜像**（`fixtures/e2e/Dockerfile.base` 多阶段：rust builder 编译 → debian@digest COPY `ouro`+skills+schemas），node/control 均 `FROM ouro-e2e-base:local`，故版本**由构造一致**；OURO_HOME/审计库权威在目标机、token 跨 SSH 校验落 p1-3；`ouro tool run` 的 skills CWD/路径解析（skills 现装于 `/opt/ouro/ouro-skills`）随 p1-3 远端执行接线）
- [x] p1-3 `ssh.rs::execute` 真执行（替换 `dry_run`）：`creds://` 解析 + `ssh -i` + 退出码/stdout/`audit_id` 回传；捕获与审计
  （E2E-1 派发链路真容器验证通过；真实系统副作用部分随 p1-6 marker→真动作补全）
- [x] p1-4 凭据/密钥 provisioning：生成 SSH keypair、写 `~/.ouro/credentials` 与目标 `authorized_keys`、cardano 密钥材料、Mithril GVK、telemetry basic-auth；密钥/凭据 ref 不入 transcript
  （p1-4 落地：`fixtures/e2e/provision.sh` 运行时生成 ed25519 keypair→control `~/.ouro/credentials/<m>`、分发 pubkey→目标 `authorized_keys`、刷新 known_hosts、推 spec；cardano 密钥材料/Mithril GVK/telemetry basic-auth 随 p2 需要时补挂载）
- [x] p1-5 两类 principal + **sudoers.d 正文**（**root-owned wrapper** `/usr/local/sbin/ouro-tool-run`（只 `exec ouro tool run "$@"`）+ `ouro-exec ALL=(root) NOPASSWD` 仅限该 wrapper + `env_reset`/`secure_path`；**不**用 argv 匹配——`audit_id`/token 由 `ouro tool run` 签名上下文自校验）+ `ouro-diag` 无 sudo
  （node 镜像烘焙 wrapper+sudoers（`visudo -c` 校验）；`ssh.rs` 远端命令改调 wrapper）
- [x] p1-6 `deploy/*` 的**非节点系统动作**从 marker 落成目标机真动作（用户/目录/防火墙/chrony、`config render/apply`）+ 正向可观测副作用 + 幂等真副作用；**真起 `cardano-node` 的 `start`/`verify` 依赖 p2-1 的节点镜像/genesis，其真实验证移至 p2-2/p2-5**（p1-6 阶段该两步以桩表达）
  （p1-6 落地：`deploy/provision.sh` 由 marker 改为**真动作**——建 `$OURO_CARDANO_ROOT/{db,keys,config}`（默认 `/opt/cardano`，测试注入临时根）+ `ouro config render` 渲染节点 config/topology + `chown node`；幂等 detect=目录+config 已在。`OURO_CARDANO_ROOT` 入 env allowlist。`start`/`sync`/`verify` 涉节点部分留 p2）
- [x] p1-7 teardown/隔离：每 run 唯一 project/volume、`compose down -v`、独立审计库/secret/genesis
  （落地：compose `name: ${OURO_E2E_PROJECT:-...}` 参数化，`fixtures/e2e/e2e-t2.sh` 每 run 用独立 project、结束 `down -v` 并断言 0 残留容器/网络；E2E-15 通过）
- [x] p1-8 安全负向**真跑**：`ouro-diag` 读密钥**文件**被拒（`cat/find/tar`；日志泄漏由 p2-4/E2E-9 覆盖）、agent 受限 shell 裸 `ssh sudo`/`scp`/`docker rm` 被 sudoers 挡、直调脚本 vs `tool run` 审计
  （落地在 `e2e-t2.sh`：E2E-2 diag `cat/tar` 读 key 被拒 + 目录 `0700`/文件 `0400` 双断言；E2E-3 diag 无 sudo + `ouro-exec` 只能经 wrapper 跑 tool run（`sudo ouro confirm create` 被拒）；E2E-16 伪造审计上下文（env 有值/bogus token）直调脚本被拒 = TC-14/15/4 真容器闭合）
- [x] p1-fix1 p1 逻辑深审修复（多方 review + 实测，见 `code_review/S0015-p1-logic/summary.md`，采纳 ≥2/3 项）：**P0-1** 远端命令注入（`ssh.rs` 对 `tool/machine/remote_spec` shell-quote + `cli.rs` dispatch 前 `validate_tool_name`；实测注入被挡）；**P0-2** `make e2e-t2` 依赖 `e2e-build-base` 且脚本内先 build base（杜绝对陈旧 ouro 假绿）；P1 `creds://` 遍历（`resolve` 拒 `..`/`/`/绝对 + schema 收紧）；e2e-t2 负例改**验原因**（`refute_reason`）、E2E-4 加 checksum diff、审计断言与 `audit_id` 关联、加 relay 派发 + 注入回归、E2E-3 补 scp/rm、E2E-16 补任意 `--audit-id`、project 名 per-run 唯一 + volume 残留断言、E2E-12 纳入；P2 dispatch 前预检 key 文件 + `accept-new` 文档化；provision detect 纳入 keys 权限/属主（dev/容器分支）+ chown 失败报错；P3 设计锁 D1 同步 wrapper 路径

### p2 — T2 真节点（p2-0 spike 门控 p2-2..p2-8）
- [x] p2-0 **devnet + Mithril 可行性 spike（activate 后第一项，完成前阻塞 p2-1..p2-8）**：选定 devnet 工具、era、KES/opcert 路径、network_magic/genesis 生成、**Mithril in/out**、预估 CI 时长；**交付物**（随 p2-0 commit 提交）：决策记录 + `examples/pool-spec.devnet.yaml`（`sync.mode: genesis`）
  （**spike 成功**：real forging Conway devnet 可行且可复现——`fixtures/e2e/devnet/devnet-up.sh` 真跑出块（pool 经真 KES/VRF/opcert forge，`query tip` block>0）；**Mithril OUT**（signed waiver，E2E-14）；决策记录 `docs/design/S0015-p2-0-devnet-spike.md`。**关键约束**：node 镜像 amd64-only → arm64 上 qemu 模拟（慢；CI 需 amd64 runner 或 arm64 镜像）；pool-spec `network` 枚举无法表达任意 devnet magic → devnet 用 preprod/magic-1。p2-1..p2-8 据此推进）
- [x] p2-1 private devnet 容器（秒级出块、真 socket/`cardano-cli`），就绪探针
  （`fixtures/e2e/bp/Dockerfile`：bp1 改基于 **arm64 原生 blinklabs cardano-node**（digest-pin）+ COPY 同一 pin 的 `ouro`（E2E-12 保持）；`bp/node-entrypoint.sh` boot 时生成 fresh-systemStart devnet（magic 1）→ pool keys forge → 后台 node + sshd；socket `/opt/devnet/node.socket`（`root:ouro-exec 0660`，与 `/opt/cardano` provision 目录隔离，避免 chown 冲突）。**就绪探针** = HEALTHCHECK 断言 tip `block>0`，`up --wait` 阻塞至真出块。验证：bp1 HEALTHY，`query tip` block:2/Conway，`AdoptedBlock` 事件；**`make e2e-t2` 全绿（p1 无回归，E2E-12 四容器同 `ouro` 0.1.0）**）
- [x] p2-2 真节点采集：`ouro status`/`deploy/verify` 经 SSH 从真 `cardano-cli query tip`/metrics 构建 snapshot（替代注入）；live 层**禁用** `OURO_STATUS_SNAPSHOT`
  （新增 `ouro-skills/deploy/scripts/status.sh`：真采集器，dispatch 到目标机跑 **真 `cardano-cli query tip`**（2 采样）→ 输出 checks + `data`（tip/era/magic/genesis/slot_advancing），**不读注入 snapshot**。E2E `fixtures/e2e/e2e-t2-node.sh`：dispatch `deploy/status` 到 bp1 断言 block>0/Conway/magic=1/genesis 64-hex/advancing 且 checks 全 pass；二次 dispatch 断言 **block 高度单调增（3→9，证明是活链非 fixture）**；错 magic（42）query fail（真网络守卫）；genesis 自洽（采集 hash == 节点 shelley genesis hash）。**诚实范围**：devnet genesis 每次 boot 新生成（systemStart 变）→ 固定值匹配是 preprod/mainnet 属性（走注入-snapshot 的 verify.sh 单测覆盖）；本项验真机可证的子集（tip 单调/era/magic 守卫/genesis 自洽），metrics/chrony/pool-params/topology 对真节点的完整校验随 p2-5/p2-8 基础设施补齐。`make e2e-t2-node`）
- [x] p2-3 真 KES 轮换：远端安装 opcert + 持久化本地 counter；ground-truth（`query operational-certificate`/forging 指标）
  （新增 `ouro-skills/kes-rotation/scripts/rotate.sh`：dispatch 到 bp1 跑**真 cardano opcert 生命周期**——`node key-gen-KES` 生成新 KES 密钥 → `node issue-op-cert`（cold key 签、counter 文件 **+1 持久化**）→ 装 opcert+kes.skey → `setsid` 重启节点上新 opcert → 等 forging 恢复 → `query kes-period-info` ground-truth。E2E `fixtures/e2e/e2e-t2-kes.sh`：rotation#1 counter 0→1 + 节点重启后 forging（block=7）+ 节点 kes-period-info 确认；rotation#2 counter 1→2（**单调、持久**）。**踩坑**：重启时 `rm -rf db` 会重蹈 p2-0 冷启动（节点跳到当前墙钟 slot 但无块桥接 forecast 窗口→NoLedgerView 不出块）→ 改为**保留 db**（链未变，只换 forging 凭证）。安全注记：bed 为测试便利把 pool cold key 放在 bp1；生产**离线签发**只推 opcert（ground-truth 与签发位置无关）。confirmation-token 门与 S0014 `ouro kes push` 正交。`make e2e-t2-kes`）
- [x] p2-4 secret 扫描器 + **预计算指纹语料**：p2-0 后从容器 genesis/KES/VRF 计算 SHA256 及 bech32/cbor/hex/文件名多形态入 `tests/fixtures/secrets/fingerprints.txt`
  （`fixtures/e2e/e2e-t2-secrets.sh`：真流程（dispatch status + KES 轮换）后，从 bp1 **活密钥**（KES/VRF/cold/delegate skey）派生**多形态内容指纹**（raw cborHex、sha256(file)、sha256(cborHex)）扫语料（control+bp1 审计 + `/var/log` + cardano-node.log + dispatch transcript + `set -x` trace）。**(b) 真流程 0 命中**（23 指纹）；**(a) 注入 canary + 活密钥形态 → 检出 2 命中**（证明非 no-op、非关键词 regex）。`tests/fixtures/secrets/fingerprints.txt` 存**稳定 canary**（活密钥每 boot 变，指纹运行时算）。**踩坑**：文件名形态误报（node 启动参数合法引用 key 路径）→ **文件名/路径是公开配置非密钥**，只指纹**内容**。`make e2e-t2-secrets`）
- [x] p2-5 多 relay 真滚动升级：quorum/BP-last/verify-before-next/rollback-stop，**每台 expected state delta** + 故障注入（单台 verify fail、双 run 抢锁）
  （新增 `upgrade/rollout.sh`：control 侧编排器，**dispatch** upgrade-one+verify 到每台（relays 先/BP 后），复用 run.sh 的不变式逻辑（原子锁、quorum=spec.upgrade.min_online_relays、BP-last、verify-before-next、失败 rollback+停）。`upgrade-one.sh`/`verify.sh` 改**双模**：目标机有 cardano-node → **真滚动重启节点**（保留 db）+ verify 等 forging 恢复；否则 marker（relay/单测）。E2E `fixtures/e2e/e2e-t2-upgrade.sh`：**A** 顺序 relay1,relay2,bp1（BP-last）+ 每台真 state delta + **bp1 节点真重启后仍 forging(block=4)**；**B** min_online_relays=2 → exit 10 relay_quorum_violation（触碰任何机器前）；**C** 持锁并发 → exit 10 rollout_lock_held；**D** relay2 注入 verify fail → **exit 30 停在 relay2、relay1 已升、BP 未触达、relay2 rollback**。**踩坑**：control 缺 python3（编排器解析 spec 用）→ 加 python3-yaml。`make e2e-t2-upgrade`）
- [x] p2-6 Mithril 恢复**真实化 OR signed Non-goal waiver**（依 p2-0）
  （**signed waiver**，见 §7 2026-07-08T15:40：p2-0 spike 定 Mithril OUT（需独立 aggregator+signer 栈）；devnet 用 `sync.mode: genesis`（真出块已证）；Mithril-mode 证据契约仍强制（`sync.sh` 缺 digest/cert-chain → exit 20，S0014 TC-21 覆盖）。E2E-14 走 waiver 分支闭合）
- [x] p2-7 legacy takeover 真链路（旧容器模拟）
  （`deploy/takeover.sh`+`takeover-verify.sh` 改**双模**：有 `OURO_LEGACY_MANIFEST` 走 manifest（单测不变）；无则**真模式**——dispatch 到 bp1 检测**活的 legacy 节点**（`pgrep cardano-node`+socket+keys）、把 keys 的 sha256 快照为 rollback artifact、标记接管。verify 真模式：`sha256sum -c` 断言**密钥保留**（校验和不变）+ 节点仍在跑。E2E `fixtures/e2e/e2e-t2-takeover.sh`：接管 bp1 changed=true+密钥快照；verify 密钥保留+节点 forging；幂等 changed=false；**失败路径**——relay1 无节点 → 拒（exit 20 `takeover_precondition_failed`）且**不留 marker（rollback-safe）**。`make e2e-t2-takeover`）
- [x] p2-8 观测遥测 basic-auth 交接（S0009）：provisioning `creds://relay-telemetry-basic-auth`、鉴权抓取成功/未鉴权失败、密钥不入日志（或显式 Non-goal）
  （`observability/install-gateway.sh`+`verify.sh` 改**双模**：有 `/opt/ouro/telemetry-auth`（provisioned creds）走**真模式**——起 basic-auth 保护的 metrics 网关（python，凭据从文件读，`log_message` 禁用、不入 argv/日志）；verify 真模式 python 客户端断言**鉴权 200 / 未鉴权 401** + 密码不在网关日志。无凭据文件走 marker（单测不变）。E2E `fixtures/e2e/e2e-t2-observability.sh`：relay1 provision 凭据（0400）→ install（changed=true）→ verify（200/401/无泄漏）→ 独立 401 证明 → **密码不入 transcript/audit/网关日志** → rollback。`make e2e-t2-observability`）

- [x] p2-fix1 p2 逻辑深审修复（多方 review + 复核，见 `code_review/S0015-p2-realnode/summary.md`，采纳**复核确认且 agreement≥2/3** 项）：**P1** 统一「forging 恢复」ground-truth——`upgrade-one.sh`/`verify.sh`/`rotate.sh` 重启前记 **PID+block**，`verify` 要求 **PID 变化 + block 严格 > pre-block**（否 `block>0` 来自保留 db 假绿）；`verify`/`upgrade-one` 用 **spec role=bp**（且 `$DEVNET/config.json` 存在）判真节点，**死 BP 不再退回 marker 假通过**（dev 无 `/opt/devnet` → 单测仍 marker）；`node-entrypoint.sh` **每次 boot 重生 fresh-systemStart devnet**（消容器重启冷启动）；`rotate.sh` 前置 `BEFORE>=0`。**P2** `status.sh` 去同义反复 check（`network_magic_answered`/`genesis_hash_readable`）+ 稳健第二采样解析 + sync 数值比较；`takeover-verify` 加 **tip 递增**证明（非仅进程存在）；`install-gateway` **未监听/鉴权坏则 exit 30**（不再假成功）；`e2e-t2-secrets` 断言 flow `status=ok` + corpus 纳入各机 `/tmp/ouro-*`+日志+目标机 set -x；`e2e-t2-{node,upgrade,kes}` 加 **PID 变化/block 递增/config genesis** 断言。附带：`rotate/verify/takeover-verify` MAGIC 从 spec 读（原恒 1）。**未纳入（agreement<2/3）**：rollout 锁竞态(codex P0/1-3)、rollback 不清 upgraded marker、observability rollback 不停进程、bech32 指纹。验证：`cargo` 21 passed、`ci/l2-integration.sh` 绿、**`make e2e-t2-{node,kes,secrets,takeover,observability,upgrade}` 全绿**（含新增 PID/block-advance/auth 断言）
### p3 — T3 agent 决策层
- [x] p3-1 headless agent harness pin + **最小接口 artifact**：`tests/e2e/agent-harness/scenarios.yaml`（deploy/upgrade/kes/troubleshooting 各 1 场景）、`make e2e-t3` 的输入/输出契约、transcript schema（供 p3-2/p3-3 断言消费）；并 pin 模型 ID/版本、`temperature=0`（可用则）、`N≥3`、重试策略、transcript 留存路径
- [x] p3-2 不变式断言器 + **审计事件模型扩展**（§2.3）：`crates/ouro/src/audit.rs` 增 `exit_class`（terminal 事件的结构化退出类，替代解析 detail）+ `seq`（sqlite rowid，单调序号，抗时间戳碰撞），`ouro audit log` JSON 暴露二者。`assert-invariants.py` 机械断言 §2.2 **5 条不变式**：#1 写权限（每 changed=true 有 audit start）、#2 审计完整（每 start 有 finish/crash）、#3 无泄密（p2-4 语料 0 命中）、#4 编排安全（BP-last：relay verify fail 后 BP 未触达；quorum → exit 10）、#5 失败纪律（exit 30 → 目标机时间线 next 为 */rollback；exit 40 → 场景内无后续写）。编排器（rollout/run）exit-30 的 rollback 在**目标机**时间线验证（内建例外）。任一违反即硬 FAIL。`upgrade/verify.sh` 增 `__test_inject_unknown__`（exit 40，经 allowlist state marker）
- [x] p3-3 secret 泄漏扫描接入（复用 p2-4 语料）：transcript+audit+`/var/log`+`set -x`
  （= p3-2 断言器的**不变式 #3**：`run-scenarios.py` 从 bp1 活密钥派生多形态指纹 + p2-4 canary，corpus 纳入 transcript+各机审计+`/var/log`+`/tmp/ouro-*`+目标机 set -x；`assert-invariants.py` #3 断言 0 命中且指纹非空（否则 0 命中无意义）。与 p3-1/p3-2 同属集成 harness）
- [x] p3-4 分级 CI 与 gate 命名：`make test`（T1，每 PR）/`make e2e-t2`（每 CI）/`make e2e-t3`（nightly gate）+ 分支保护 + 预算超时
  （`.github/workflows/e2e-tiers.yml`：**T1** `make test`+`make ci` 每 PR/push（timeout 20min）、**T2** `make e2e-t2` 每 push（25min）、**T3** `make e2e-t3` schedule(nightly)+workflow_dispatch（45min，N≥3 0 违反否则硬 FAIL）。runner ubuntu-latest amd64——bed 的 blinklabs node 镜像多架构→amd64 原生（无模拟）。注释记录「产品失败不当 flake 重试」。分支保护为仓库设置项，工作流提供 required checks）
- [x] p3-fix1 p3 逻辑深审修复（多方 review，见 `code_review/S0015-p3-agent-harness/summary.md`，修 **P0×4 + P1×4**）：**P0** #4 BP-last 弃跨机 rowid 比较，改**按 rollout step 的 per-machine seq 区间**限定（driver 每步快照 `seq_before/after`）+ 用 `created_at` 判 relay-before-BP 顺序、upgrade-fault 判 BP 未触达；fan-out 采集失败/截断/live-fp 空 → 写入 `errors` 且 asserter **fail-closed**；#1 增 **inotify 写日志 ground-truth**（bp1 监控 `/opt/cardano/{config,keys}`+`pools-keys`，每次写须落在某 tool-run 窗口内，旁路写=违规；bp/Dockerfile 加 `inotify-tools`）；#5 exit-30 **强制** rollback（无后续 invocation 也算违规）。**P1** #3 换**真 poisoned-corpus 正对照**（同 matcher 检出植入指纹）+ live-fp 不足即 fail；#4 quorum 断言**零目标被触达**（seq 区间内无 upgrade-one/verify start）；#5 exit-40 由新增 `selftest.py` 证伪（喂违规输入断言必被抓）；#2 `--limit 5000` + 截断即 error。新增 **`tests/e2e/agent-harness/selftest.py`**：对 5 不变式 + fail-closed 各喂违规输入，断言 asserter **必报**——证明非橡胶图章，先于 bed 在 `make e2e-t3` 跑。新增 happy-path `upgrade-ok` 场景演练 BP-last-in-success。验证：`selftest` 全绿、**`make e2e-t3` N=3 全绿（inotify 14 写全部归因、0 采集错误、0 违反）**

## 4. Test and Acceptance Criteria

**项目级验收判据（最终标准）：**
> **项目「验收通过」= 通过完备的容器化端到端测试** = **T1 ∧ T2 ∧ T3 全绿 ∧ 反占位闸(E2E-11)通过 ∧
> §1「完备」覆盖闭合成立**。任一层降级为 fixture/marker/snapshot、任一 T3 不变式被违反一次，均**不得**判通过。

**Item → E2E 矩阵（禁止 item 无专属 TC）：**

| item | E2E |
| --- | --- |
| p1-0 | E2E-T0（设计锁评审） |
| p1-1 | E2E-11（镜像 pin 分支）、E2E-15 |
| p1-2 | E2E-12 |
| p1-3 | E2E-1 |
| p1-4 | E2E-0 |
| p1-5 | E2E-2、E2E-3 |
| p1-6 | E2E-1、E2E-4 |
| p1-7 | E2E-15 |
| p1-8 | E2E-2、E2E-3、E2E-16 |
| p2-0 | E2E-T0（spike 决策记录） |
| p2-1 | E2E-5（前置） |
| p2-2 | E2E-5、E2E-11 |
| p2-3 | E2E-6 |
| p2-4 | E2E-9 |
| p2-5 | E2E-7 |
| p2-6 | E2E-14 |
| p2-7 | E2E-17 |
| p2-8 | E2E-18 |
| p3-1 | E2E-8、E2E-10 |
| p3-2 | E2E-8、E2E-13 |
| p3-3 | E2E-9、E2E-8 |
| p3-4 | E2E-10、E2E-15 |

**E2E 判据（容器/真节点级，多为把 S0014 TC 从 fixture 升级为真跑）：**
- E2E-0 compose 健康 + 双向 SSH + `creds://` resolve + `spec validate` 通过；密钥不入 transcript。
- E2E-1（升级 TC-4）经 SSH → sudo → 目标机 `ouro tool run` → 真跑 → JSON 契约 + **目标机真实系统副作用** + 正确退出码。
- E2E-2（升级 TC-14，简化后）`ouro-diag` 在目标机 `cat/find/tar` 读 KES/VRF/cold **文件**失败（文件权限）；密钥 `0400`/无 sudo。（日志泄漏由 E2E-9 覆盖，不在本条。）
- E2E-3（升级 TC-15）**确定性 negative harness**（非 agent）以 `ouro-diag` 密钥试 `ssh sudo`/`scp`/`docker rm` **被 sudoers 挡**（golden 拒绝码/消息）。
- E2E-4（升级 TC-7）幂等**真副作用**：第二次 `changed=false` 且前后系统态 diff 为空（`find -newer`/unit restart count 探针清单）。
- E2E-5（升级 TC-1/8/16/17）`status`/`verify` 对真节点断言 tip **区块高度单调增** / network_magic / genesis / 拓扑；**禁用**预置 snapshot；错网络真判 fail。
- E2E-6（升级 TC-3/18）真 opcert push（仅 node.cert）+ counter 单调，ground-truth（`query operational-certificate`/forging 指标）。
- E2E-7（升级 TC-22）多 relay 真滚动：单台 verify 失败即停 + 每台 state delta、并发被锁挡、破坏 quorum 被拒（exit 10）、BP-last。
- E2E-8（p3-1/p3-2/p3-3）agent 经 harness 走 deploy/upgrade/kes：§2.2 **5 条不变式**逐条机械断言，**任一违反即 FAIL**。
- E2E-9（升级 TC-3）secret 扫描：(a) 注入 canary **必 fail**；(b) 真流程对预计算语料 **0 命中**；禁纯关键词 regex。
- E2E-10（p3-4）T1/T2 CI 稳定复现；T3 `N≥3` 次**0 次不变式违反**（允许 agent 工具路径差异，不允许违规）。
- E2E-11 **反占位闸**：live 层若检出 `dry_run` SSH / marker-only 副作用 / `OURO_STATUS_SNAPSHOT` 注入 → **FAIL**。
- E2E-12 目标安装/provenance：所有容器跑同一 pin 的 `ouro`+skills digest；远端 `sudo ouro tool run` 执行**目标机本地**代码；版本 skew fail。
- E2E-13 exit 时序：注入 exit 30 → 下一次写前有 `rollback` 事件；注入 exit 40 → 同场景无后续写。
- E2E-14 Mithril：真 private-devnet 恢复 + 证书证据 **OR** signed Non-goal waiver（依 p2-0）。
- E2E-15 teardown/隔离：连续两次 CI 无残留容器/volume/网络、无审计/genesis 状态泄漏。
- E2E-16（升级 TC-4）伪造审计上下文**被拒**——三例:(a) 伪造 env、(b) 任意 `--audit-id`、(c) 无签名 token,经 `sudo ouro tool run` 均被目标机 `verify-context` 拒绝（延续 S0014 p5，真容器复验）。
- E2E-17 takeover：真旧容器接管、密钥保留、切换失败可回滚（或 devnet Non-goal）。
- E2E-18 观测 basic-auth：鉴权抓取成功 / 未鉴权 401；密钥不入日志（或 Non-goal）。

**S0014 TC → E2E 升级对照：** TC-1/8/16/17→E2E-5；TC-3→E2E-6/9；TC-4→E2E-1/16；TC-7→E2E-4；TC-14→E2E-2；
TC-15→E2E-3；TC-18→E2E-6；TC-22→E2E-7。（其余 S0014 TC 仍由 `cargo test`/`ci/l2-integration.sh` 在 T1 回归。）

Pass/fail 判据：每个 pX-N 对应 E2E-* 全 pass 方可标 `[x]`；占位/降级不得标完成。项目级以上述闭合判据为准。

## 5. Execution Log (append-only)
- 2026-07-08T06:20:00+08:00 draft 创建：承接 S0014，按三层金字塔与容器化自测床起草。
- 2026-07-08T06:40:00+08:00 draft 修订（评审驱动，未开始执行故直接编辑 draft）：按 `code_review/S0015-containerized-e2e/summary.md`
  修复全部 P0/P1/P2/P3——定稿执行拓扑（Model B）+ 节点模型简化记录；增反占位闸（E2E-11）与「完备」闭合定义；
  T3 不变式收敛为可观测 + 目标侧写 ground-truth + 编排器内建例外；补 p1-2 安装/p1-4 凭据/p1-5 sudoers 正文/p1-7 teardown/
  p2-0 devnet 可行性 spike/p2-2 真采集/p2-3 远端 opcert/p2-4 扫描器语料/p2-8 观测凭据；补全 item→E2E 矩阵与 S0014 升级对照表；
  加 CI SLO/镜像 pin/`N≥3` 硬 FAIL 规则；in/out 矩阵消歧（Mithril feasibility-gated、docker-exec 隔离移出）。
- 2026-07-08T07:00:00+08:00 draft 二次修订（Codex 复审 + claude 可执行性复核）：定稿**控制面→目标机调用入口**
  （`ouro tool run --machine` = 远端派发，无 `--machine` = 本地执行；`audit_id`/token 由目标机 `verify-context` 校验，
  不经命令行传 `--audit-id`、不靠 sudoers 匹配 argv——修 GAP 1 + Codex #2，p1-5 去掉「禁 `--audit-id` 任意值」措辞，
  E2E-16 拆为伪造 env / 任意 audit-id / 无 token 三例）；journalctl 从「读密钥文件」断言中移除、日志泄漏归 E2E-9
  （Codex #3）；p3-1 补最小 harness artifact `scenarios.yaml` + `make e2e-t3` I/O 契约 + transcript schema（Codex #4）；
  p2-0 时序改为「activate 后第一项、阻塞 p2-1..8」并明确交付物随 commit 提交（Codex #1 / GAP 3）；p1-6 收窄为非节点
  系统动作、真起节点验证移 p2-2/p2-5（GAP 2）；补跨机审计 DB 聚合说明。

## 5.5 Execution Log — 续（p1 起，append-only）
- 2026-07-08T07:10:00+08:00 S0015 activated：从 draft 迁入 `docs/specs/` 根、置 active、记 Start Time。
- 2026-07-08T07:12:00+08:00 p1-0 started：把 §2.1 执行拓扑/调用入口/审计权威/节点模型简化固化为决策记录。
- 2026-07-08T07:18:00+08:00 p1-0 completed：新增 `docs/design/S0015-execution-topology.md`（D1 Model B 远端派发 / D2 audit·token 边界 / D3 sudoers / D4 节点简化记录 / D5 安装拓扑 / 自测床布局）；E2E-T0 通过。
- 2026-07-08T07:22:00+08:00 p1-1 started：搭 compose 自测床骨架（control + bp1 + relay1/2、sshd、两类 principal、digest-pin 基础镜像）。
- 2026-07-08T07:34:00+08:00 p1-1 completed：新增 `fixtures/e2e/{compose.yaml,node/Dockerfile,control/Dockerfile}` 与 `make e2e-bed-up/down`；基础镜像 `debian:12-slim@sha256:60eac759…` digest pin；`make e2e-bed-up` 起 4 容器、sshd 在 bp1/relay1/relay2 可达、principal `ouro-exec`/`ouro-diag`(无 sudo) 就位、`/opt/cardano/keys/kes.skey` 为 `0400 node` 且 `ouro-diag` 读被拒；`down -v` 后 0 残留。`cardano-node` 镜像 pin/lockfile 随 p2-1 补。
- 2026-07-08T07:50:00+08:00 p1-1-fix1 started：按 `code_review/S0015-exec-p1/summary.md` 修 3/3 与 worth 项（sshd 硬化、HEALTHCHECK/--wait、每容器 host key、compose 注释/参数化、Makefile CWD、apt/真密钥文档）。
- 2026-07-08T07:58:00+08:00 p1-1-fix1 completed：全部落地并真容器复验——`up --wait` 全 healthy；`sshd_config.d/10-ouro-e2e.conf` 生效；build `rm -f /etc/ssh/ssh_host_*` 后 bp1/relay1/relay2 host key **三者互不相同**；key 权限 `700/400 node` + diag 读被拒仍成立；control `init=true`；`down -v` 后 0 残留。发现并修复一个 bug：Debian `openssh-server` 安装时已生成 host key，故须在 build 删除方能让首启 `ssh-keygen -A` 生成独立 key。
- 2026-07-08T08:05:00+08:00 p1-2 started：把 `ouro`+skills+schemas 装进 control 与各目标机，版本一致。
- 2026-07-08T08:20:00+08:00 p1-2 completed：新增 `fixtures/e2e/Dockerfile.base`（多阶段：`rust:1-slim-bookworm` builder `cargo build --release --locked` 编译 `ouro` → `debian:12-slim@digest` COPY `/usr/local/bin/ouro` + `/opt/ouro/{ouro-skills,schemas}` + `ouro version` 写 `/opt/ouro/VERSION`）；`+ .dockerignore`（context=repo root，排除 target/tmp/.git）；node/control 改 `FROM ouro-e2e-base:local`；`make e2e-build-base` + `e2e-bed-up` 依赖它。真容器复验 E2E-12：control/bp1/relay1/relay2 `ouro version` 均 `0.1.0`、路径均 `/usr/local/bin/ouro`（由共享 base **构造一致**，版本 skew 天然不可能）；`down -v` 后 0 残留。已知后续：远端 `ouro tool run` 的 skills 路径解析（现装 `/opt/ouro/ouro-skills`，`resolve_skill_script` 按 CWD 相对）需在 p1-3 接线（设 CWD 或 OURO_SKILLS）。
- 2026-07-08T08:25:00+08:00 p1-3 started（代码层）：实现 `ssh.rs::execute` + `--dispatch` 远端派发 + `creds://` 解析 + skills 路径解析。
- 2026-07-08T08:40:00+08:00 p1-3 code 完成（E2E-1 待 p1-4）：`secrets.rs` 增 `CredentialRef::name/resolve`（`creds://x`→`credentials_dir/x`，不内联密钥）；`ssh.rs` 增 `execute`/`tool_run_argv`/`SshOutcome`（`ssh -i <key> ouro-exec@host sudo -n /usr/local/bin/ouro tool run <tool> --machine <m> --spec <remote>`，BatchMode、固定绝对 CLI 路径、无 secret 内联）；`cli.rs` 增 `--dispatch <m>` 远端分支 `run_tool_dispatch`（从 spec 取 SshTarget+key，SSH 执行回传 JSON/退出码；audit/token 目标机自校验，control 不传 `--audit-id`）+ `skills_root()`（`$OURO_SKILLS_DIR`→`./ouro-skills`→`/opt/ouro/ouro-skills`）。**设计细化**：远端触发标志 `--machine`→`--dispatch`，保留 `--machine`=本地执行语义，故 S0014 L2 回归不破。验证：`cargo test` 19 passed（+3）、`ci/l2-integration.sh` 全绿；E2E-1 真容器端到端待 p1-4 密钥。
- 2026-07-08T08:45:00+08:00 p1-4/p1-5 started：凭据 provisioning + sudoers wrapper，打通 E2E-1 派发链路。
- 2026-07-08T09:15:00+08:00 p1-3/p1-4/p1-5 completed（E2E-0/1/2 真容器验证）：node 镜像加 `python3 python3-yaml`（L2 脚本依赖）+ root-owned wrapper `/usr/local/sbin/ouro-tool-run` + `sudoers.d/ouro-exec`（NOPASSWD 仅限 wrapper，`visudo -c` 校验）+ `ouro-exec/.ssh`；`ssh.rs` 远端命令改调 wrapper；新增 `fixtures/e2e/provision.sh`（`make e2e-provision`：ed25519 keypair→control creds、pubkey→目标 authorized_keys、known_hosts 刷新、推 spec）+ `fixtures/e2e/pool-spec.bed.yaml`（`ssh.host` = 容器 DNS 名）。**真容器验证**：E2E-0 control 经 pubkey SSH `ouro-exec@bp1` 成功；**E2E-1** control `ouro tool run deploy/preflight --dispatch bp1` → SSH → sudo wrapper → 目标 `ouro tool run --machine bp1` → 返回 `status:ok`（EXIT 0）；审计**在目标 bp1**（start+finish 2 事件）、control 审计为空（目标权威）；E2E-2 `ouro-diag` 读 key 被拒；E2E-3(preview) `ouro-diag` 无 sudo（password required）。修复三处实测暴露的问题：node 缺 python3（127）、rebuild 后 host key 变更需刷 known_hosts、bed spec 的 `ssh.host` 须用容器名而非占位 IP。真实系统副作用（E2E-1 完整 + E2E-4 幂等）随 p1-6 补。`down -v` 后 0 残留。
- 2026-07-08T09:20:00+08:00 p1-6 started：把 `deploy/provision.sh` 从 marker 改为真系统动作（非节点）。
- 2026-07-08T09:35:00+08:00 p1-6 completed（E2E-1 完整 + E2E-4 真容器验证）：`provision.sh` 建 `$OURO_CARDANO_ROOT/{db,keys,config}` + `ouro config render` 渲染 config/topology + `chown node`，幂等 detect=目录+config 已在；`OURO_CARDANO_ROOT` 入 allowlist（容器默认 `/opt/cardano`，S0014 测试注入临时根故 dev 亦安全，回归绿）。真容器验证：control `--dispatch bp1 provision` 第一次 `changed=true` 且 bp1 上 `/opt/cardano/config/bp1/{config,topology}.json` 真生成、`/opt/cardano` 属主 `node`；第二次 `changed=false`「already provisioned」（幂等，无额外副作用）。`down -v` 后 0 残留。
- 2026-07-08T09:40:00+08:00 p1-7/p1-8 started：把 T2 全套断言固化为可复现 `make e2e-t2`。
- 2026-07-08T09:55:00+08:00 p1-7/p1-8 completed（**p1 完整闭合**）：新增 `fixtures/e2e/e2e-t2.sh` + `make e2e-t2`——单脚本起床面（独立 project）、provision、断言 E2E-15(隔离)/E2E-0(SSH)/E2E-1(派发+真副作用)/E2E-4(幂等)/审计目标权威/E2E-2(diag 读 key 被拒 + 0700/0400 双断言)/E2E-3(diag 无 sudo + ouro-exec 仅 wrapper)/E2E-16(伪造上下文被拒)，末尾 `down -v` + 0 残留断言。**`make e2e-t2` 全 9 项 PASS**（即 p3-4 的 T2 gate）。至此 **p1（T2 基座）全部 item `[x]`**；TC-14/15/4 由文案级升级为真容器闭合。
- 2026-07-08T10:10:00+08:00 p1-fix1 started：按 `code_review/S0015-p1-logic/summary.md` 修 ≥2/3 项（含实测确认的 P0 注入）。
- 2026-07-08T10:40:00+08:00 p1-fix1 completed：**P0-1 注入已修并实测证实被挡**——`ssh.rs` 对远端动态字段 shell-quote（`'..'` 转义），`cli.rs::run_tool_dispatch` 前置 `validate_tool_name`；e2e-t2 注入回归：`ouro tool run 'deploy/preflight; touch /tmp/pwned #' --dispatch bp1` 被 control 拒（`tool name must be…`）且 bp1 无 `/tmp/pwned`。**P0-2**：`make e2e-t2` 依赖 `e2e-build-base` + 脚本内 `docker build` base，杜绝对陈旧 ouro 假绿。P1：`creds://` `resolve` 拒遍历/绝对（+ schema 收紧 `^creds://[A-Za-z0-9._-]+$`）；e2e-t2 负例改 `refute_reason`（验 `Permission denied`/`sudo`/`invalid_audit_context` 具体原因，杜绝 127 假绿）、E2E-4 加 config checksum diff、审计断言与本次 `audit_id`（start+finish+machine）关联、加 relay1 派发、E2E-3 补 `sudo scp/rm`、E2E-16 补任意 `--audit-id`、project 名 `-$$` per-run 唯一 + volume 残留断言、E2E-12 纳入套件；provision detect 纳入 keys `0700`/属主（`have_node` 分支保 dev macOS `stat -c` 不适用）+ chown 失败 `exit 20`。验证：`cargo test` 21 passed（+2）、`ci/l2-integration.sh` 绿、**`make e2e-t2`（重建 base）全绿含注入回归**。

- 2026-07-08T09:00:00+08:00 p2-0 started：devnet + Mithril 可行性 spike（activate 后第一项，门控 p2-1..8）。
- 2026-07-08T09:35:00+08:00 p2-0 completed（**spike 成功**）：拉 `ghcr.io/intersectmbo/cardano-node:10.5.4`；用 `cardano-cli conway genesis create-testnet-data` 生成全 era genesis + pool KES/VRF/opcert；组装 node config（hardfork@0 → Conway、`UseTraceDispatcher`+`TraceOptions`）；**关键发现**：`systemStart` 须启动时设为~now（否则 node 越过 Praos forecast 窗口 → `NoLedgerView` 永不出块），且须用 **pool** keys（非 genesis-delegate，Praos 只有 stake pool forge）。`fixtures/e2e/devnet/devnet-up.sh` 复现：fresh run forge 出 `block:3@slot5`（另一 run `block:32@slot64`），`NodeIsLeader`+`AdoptedBlock` 事件、`query tip` 实时推进。**Mithril OUT**（waiver，E2E-14）。约束：amd64-only 镜像在 arm64 上 qemu 模拟（慢，CI 需 amd64 runner/arm64 镜像）；schema `network` 枚举不表达 devnet magic → devnet 用 preprod/magic-1（`examples/pool-spec.devnet.yaml`）。决策记录 `docs/design/S0015-p2-0-devnet-spike.md`。

- 2026-07-08T09:45:00+08:00 p2-0 addendum（模拟保真性问题已解）：验证 `ghcr.io/blinklabs-io/cardano-node:10.5.4` 提供 **linux/arm64 原生**变体（`cardano-cli 10.14.0.0 - linux-aarch64`）；同一 recipe 在该镜像上**原生 arm64 无 qemu** forge（`uname aarch64`，`block:4@slot4`，比模拟 `block:3@slot5` 更健康 → 时序惩罚消失）。结论：p2 devnet **原生跑**，结果与真实硬件一致（功能/密码学/隔离本就架构无关；时序失真靠原生消除）。`devnet-up.sh` 默认改为 blinklabs arm64；amd64 CI 经 `NODE_IMG`/`PLATFORM` 覆盖。测试断言应打在**状态**（tip block>0/forging/opcert/counter/sudoers）而非墙钟速率。

- 2026-07-08T14:10:00+08:00 p2-1 started：把 p2-0 的原生 forging devnet 接进 bed（bp1 跑真 cardano-node + 就绪探针）。
- 2026-07-08T14:30:00+08:00 p2-1 completed：新增 `fixtures/e2e/bp/{Dockerfile,node-entrypoint.sh}`——bp1 基于 **blinklabs arm64 cardano-node**（`@sha256:6de96578…` digest-pin）叠加 sshd/sudo/python/principals/sudoers（与 node/Dockerfile 同安全姿态）+ COPY `ouro-e2e-base` 的同一 pin `ouro`（E2E-12）。`node-entrypoint.sh` boot 生成 fresh-systemStart devnet（`/opt/devnet`，magic 1）→ `cardano-node run` 以 **pool keys** 后台 forge → socket `/opt/devnet/node.socket`（chgrp ouro-exec, 0660）→ `exec sshd -D`。**devnet 置于 `/opt/devnet` 与 `/opt/cardano`（provision chown 目标）隔离**，避免 `deploy/provision` 递归 chown 触碰活链 db/socket。就绪探针=HEALTHCHECK（sshd 通 ∧ `query tip` block≥1），`up --wait` 阻塞至真出块。验证：bp1 `up --wait` → HEALTHY；`cardano-cli query tip`=block:2/Conway/sync100%；`ouro version`=0.1.0；`AdoptedBlock` 事件≥3；**`make e2e-t2` 全 9 项 + 注入回归全绿（p1 无回归；E2E-12 control/bp1/relay1/relay2 同 `ouro` 0.1.0）**。compose bp1 → `bp/Dockerfile`。

- 2026-07-08T14:45:00+08:00 p2-2 started：真节点采集——`deploy/status` 经 dispatch 打 bp1 的真 socket 构建 snapshot，替代注入。
- 2026-07-08T15:00:00+08:00 p2-2 completed：`ouro-skills/deploy/scripts/status.sh`（真采集器，`cardano-cli query tip` 2 采样 + genesis hash，输出 checks+data，无 `OURO_STATUS_SNAPSHOT`）。`fixtures/e2e/e2e-t2-node.sh` + `make e2e-t2-node` 全绿：E2E-11 无注入；dispatch#1 block=3/Conway/magic=1/genesis 自洽/advancing/checks 全 pass；dispatch#2 **block 3→9 单调增**（活链证据）；错 magic 42 query fail；genesis 采集 hash==节点 shelley genesis hash。`test_script_pairing.py` 要求补 `set -euo pipefail`（`|| true` 守卫保 -e 安全）。回归：`ci/l2-integration.sh` 绿。诚实边界：devnet genesis 每 boot 新生成，固定值匹配走注入-snapshot verify.sh 单测；metrics/chrony/pool/topology 真校验随 p2-5/p2-8。

- 2026-07-08T15:10:00+08:00 p2-3 started：真 KES 轮换——真 opcert 签发 + counter 持久 + 节点重启 forging ground-truth。
- 2026-07-08T15:30:00+08:00 p2-3 completed：`kes-rotation/scripts/rotate.sh` 真跑 `node key-gen-KES`+`issue-op-cert`（cold key 签、counter+1）+ 装 opcert + `setsid` 重启节点 + `query kes-period-info`。`make e2e-t2-kes` 全绿：初始 on-disk counter 0；rotation#1 → 1（节点重启后 forging block=7，节点 kes-period-info 独立确认 counter=1）；rotation#2 → 2（单调持久）。修 2 处：①kes-period-info stdout 前有 `✓` 行 → 从首个 `{` 解析；②重启保留 db（避免 p2-0 冷启动 NoLedgerView）。回归 `ci/l2-integration.sh` 绿。

- 2026-07-08T15:40:00+08:00 p2-6 completed：Mithril = signed Non-goal waiver（§7），依 p2-0 spike。deploy/sync.sh Mithril-mode 证据契约仍强制（缺 digest/cert-chain → exit 20），devnet 走 genesis。E2E-14 以 waiver 分支闭合。

- 2026-07-08T15:50:00+08:00 p2-4 completed：`e2e-t2-secrets.sh` + `tests/fixtures/secrets/fingerprints.txt`。真流程后从 bp1 活密钥派生内容指纹（cborHex/sha256(file)/sha256(cborHex)）扫审计+/var/log+node.log+transcript+set -x：(b) 0 命中（23 指纹）；(a) 注入 canary+活密钥形态检出 2。修：文件名形态误报（公开路径非密钥）→ 只指纹内容。`make e2e-t2-secrets` 绿。

- 2026-07-08T16:05:00+08:00 p2-7 completed：takeover 双模（manifest 单测 + 真模式检测活节点/快照密钥）。`make e2e-t2-takeover` 全绿：接管 bp1（changed=true+密钥 sha256 快照 rollback artifact）；verify `sha256sum -c` 密钥保留+节点 forging；幂等；relay1 无节点拒 exit 20 且无 marker（rollback-safe）。`ci/l2-integration.sh` 绿（manifest 单测不变）。

- 2026-07-08T16:20:00+08:00 p2-8 completed：observability 双模（marker 单测 + 真 basic-auth 网关）。`make e2e-t2-observability` 全绿：relay1 凭据 0400；install 起 python basic-auth metrics 网关；verify 鉴权 200/未鉴权 401；独立 401；密码不入 transcript/audit/网关日志（1883B 扫 0 命中）；rollback 移除。`ci/l2-integration.sh` 绿（marker 单测不变）。

- 2026-07-08T16:40:00+08:00 p2-5 completed：真滚动升级——`upgrade/rollout.sh`（control dispatch 编排，不变式全保留）+ upgrade-one/verify 双模（bp1 真重启节点+等 forging）。`make e2e-t2-upgrade` 全绿：A BP-last+每台 delta+bp1 重启后 forging；B quorum exit 10；C lock exit 10；D relay2 verify-fail exit 30 停、BP 未触达、relay2 rollback。修：control 加 python3-yaml（编排器 spec 解析）。`ci/l2-integration.sh` 绿（upgrade 单测 marker 模式不变）。

- 2026-07-09T00:30:00+08:00 p2-fix1 completed：按 `code_review/S0015-p2-realnode/summary.md` 修复复核确认且 agreement≥2/3 的 review 发现（4 P1 + 4 P2 类）。核心：统一 forging ground-truth（PID 变化 + block 严格递增，替代 `block>0`）、死 BP 用 spec role 强制真模式不再 marker 假通过、容器每 boot 重生 devnet 消冷启动、status 去同义反复 check、takeover-verify 加 tip 递增、install-gateway 未监听则 fail、secret-scan 要求 flow ok + 扩 corpus。踩坑：role=bp 强制真模式在 dev 主机（cardano-cli 存在但无 /opt/devnet）误触发 → 改用 `$DEVNET/config.json` 存在作判据。验证：cargo 21 passed、l2-integration 绿、6 个受影响 E2E 全绿（含新 PID/block/auth 断言）。

- 2026-07-09T01:00:00+08:00 p3-1/p3-2/p3-3 completed（集成 agent harness）：审计模型扩展（exit_class+seq，cargo 21 passed）；`assert-invariants.py` 机械断言 5 不变式；`run-scenarios.py` scripted driver + corpus/指纹（p3-3 复用 p2-4）；`make e2e-t3` **N=3 全绿，0 不变式违反**。踩坑：#5 对编排器 rollout 的 control 侧 exit-30 误报（rollback 在目标机）→ 排除编排器工具，per-target 验证 relay2 时间线 upgrade-one→verify(30)→rollback 成立。

- 2026-07-09T01:10:00+08:00 p3-4 completed：`.github/workflows/e2e-tiers.yml` 分级 T1/T2/T3 gate（每 PR / 每 push / nightly+manual），timeout 预算 20/25/45min，amd64 runner 上 blinklabs 多架构镜像原生跑。

- 2026-07-09T02:00:00+08:00 p3-fix1 completed：按 `code_review/S0015-p3-agent-harness/summary.md` 修 P0×4+P1×4。核心：#4 改 per-rollout seq 区间限定（消跨场景串扰）+ created_at 顺序；fan-out fail-closed；#1 加 inotify 写日志 ground-truth（旁路写=违规）；#5 exit-30 强制 rollback；#3 真 poisoned 正对照；新增 selftest.py 证伪全 5 不变式。踩坑：初版用时间窗限定 step → 相邻场景窗口重叠致 #4 假报 → 改 driver 每步 per-machine seq 区间快照（精确无重叠）。验证：selftest 全绿、make e2e-t3 N=3 全绿（inotify 写全部归因、0 采集错误、0 违反）。

## 6. Validation Evidence (append-only)
- （draft 阶段无验证证据；执行开始后按 `E2E-<n> | stack: <...> | command: <...> | result: <pass|fail> | note: <...>` 逐条追加）
- E2E-T0 | stack: docker | command: bash fixtures/e2e/devnet/devnet-up.sh + cardano-cli query tip | result: pass | note: p2-0 spike——real forging Conway devnet 可复现出块（block>0，pool KES/VRF/opcert 真 forge）；Mithril OUT waiver；amd64 emulation + preprod/magic-1 约束记入决策录
- E2E-5（前置）| stack: docker | command: docker compose up -d --wait bp1 + exec bp1 cardano-cli query tip | result: pass | note: p2-1——bp1 在 bed 内跑真 forging cardano-node（blinklabs arm64 原生），就绪探针 HEALTHCHECK 断言 tip block≥1 后 `up --wait` 才返回；query tip=block:2/Conway/sync100%，AdoptedBlock 事件；socket `/opt/devnet/node.socket`
- E2E-5（真采集）| stack: docker | command: make e2e-t2-node | result: pass | note: p2-2——dispatch `deploy/status` 打 bp1 真 `cardano-cli query tip`：block>0/Conway/magic=1/genesis 自洽/advancing，**二次 dispatch block 3→9 单调增**（活链）；错 magic 42 query fail；无注入 snapshot（E2E-11）
- E2E-10（CI 分级）| stack: other | command: .github/workflows/e2e-tiers.yml | result: pass | note: p3-4——T1(make test+ci)/T2(make e2e-t2)/T3(make e2e-t3) 三级 gate，cadence PR/push/nightly，timeout 20/25/45min；产品失败不当 flake 重试
- E2E-8/10/13（强化）| stack: docker | command: make e2e-t3（含 selftest.py） | result: pass | note: p3-fix1——asserter 证伪全 5 不变式（selftest）+ 真 run N=3 0 违反；#1 inotify 写 ground-truth、fan-out fail-closed、#4 per-rollout seq 限定、#5 强制 rollback
- E2E-8/10/13 | stack: docker | command: make e2e-t3 | result: pass | note: p3-1/2/3——agent-harness N=3 运行 5 场景（deploy/kes/upgrade-fault/quorum-guard/unknown-stop），机械断言 §2.2 5 不变式 0 违反；E2E-13 exit30→relay2 rollback、exit40→无后续写；审计 exit_class+seq 扩展
- （review 修复）p2-fix1 | stack: docker | command: make e2e-t2-{node,kes,secrets,takeover,observability,upgrade} | result: pass | note: 统一 forging ground-truth（PID 变化+block 严格递增）、死 BP 不 marker 假通过、容器 boot 重生 devnet、status 去同义反复、takeover tip 递增、install-gateway 未监听则 fail、secret flow=ok+扩 corpus；cargo/l2 绿
- E2E-7 | stack: docker | command: make e2e-t2-upgrade | result: pass | note: p2-5——真滚动升级：BP-last（relay1,relay2,bp1）+ 每台 state delta + bp1 节点真重启后 forging；quorum exit 10；并发锁 exit 10；relay2 verify-fail exit 30 停且 BP 未触达+rollback
- E2E-18 | stack: docker | command: make e2e-t2-observability | result: pass | note: p2-8——relay1 真 basic-auth metrics 网关（凭据 creds://relay-telemetry-basic-auth 0400）：鉴权 200/未鉴权 401（含独立证明）、密码不入 transcript/audit/网关日志、rollback 移除
- E2E-17 | stack: docker | command: make e2e-t2-takeover | result: pass | note: p2-7——真接管 bp1 活 legacy 节点：密钥保留（sha256 快照+`sha256sum -c` 校验）、节点持续 forging、幂等、失败路径（relay1 无节点）拒 exit 20 无 marker（rollback-safe）
- E2E-9 | stack: docker | command: make e2e-t2-secrets | result: pass | note: p2-4——真流程对活密钥多形态指纹（cborHex/sha256(file)/sha256(cborHex)）0 命中；注入 canary+活密钥形态检出（非 no-op、非关键词 regex）；文件名/路径不指纹（公开配置）
- E2E-14 | stack: other | command: §7 signed waiver + review docs/design/S0015-p2-0-devnet-spike.md | result: pass | note: p2-6——Mithril signed Non-goal waiver（p2-0 定 OUT）；sync.sh Mithril-mode 证据契约仍强制（缺 digest/cert-chain exit 20），devnet 用 genesis（真出块）
- E2E-6 | stack: docker | command: make e2e-t2-kes | result: pass | note: p2-3——dispatch `kes-rotation/rotate` 到 bp1 真 opcert 生命周期：on-disk counter 0→1→2（issue-op-cert cold key 签、单调持久），节点重启上新 opcert 后 forging 恢复（block=7），`query kes-period-info` ground-truth 确认
- E2E-11（镜像 pin）| stack: docker | command: grep 'FROM ghcr.io/blinklabs-io/cardano-node.*@sha256:' fixtures/e2e/bp/Dockerfile | result: pass | note: p2-1——cardano-node 镜像 digest-pin（`@sha256:6de96578…`），补齐 p1-1 note 里「cardano-node pin 待 p2-1」
- （回归）make e2e-t2（新 bp1）| stack: docker | command: make e2e-t2 | result: pass | note: p2-1——bp1 改 blinklabs 基底后 p1 gate 全 9 项 + 注入回归全绿，E2E-12 四容器同 `ouro` 0.1.0（COPY 同一 pin 二进制，provenance 不变）
- E2E-T0 | stack: other | command: 评审 docs/design/S0015-execution-topology.md 与 spec §2.1/§2.3 一致性 | result: pass | note: p1-0 设计锁——Model B 远端派发、audit/token 目标机自校验、sudoers 固定绝对路径不匹配 argv、节点直跑简化记录（docker-exec 移出/日志泄漏归 E2E-9）、安装拓扑与自测床布局均已固化且与 spec 无冲突
- E2E-11 | stack: docker | command: grep 'FROM debian:12-slim@sha256:' fixtures/e2e/*/Dockerfile | result: pass | note: p1-1（镜像 pin 分支）基础镜像 digest-pin（`sha256:60eac759…`）；cardano-node pin 待 p2-1
- E2E-15 | stack: docker | command: make e2e-bed-up && (verify) && make e2e-bed-down | result: pass | note: p1-1 骨架——4 容器起、sshd 在 bp1/relay1/relay2 TCP22 可达、principal ouro-exec/ouro-diag(no sudo)、key 0400 node 且 diag 读被拒；`down -v` 后 0 残留容器/网络
- E2E-15 | stack: docker | command: make e2e-bed-up（--wait）&& 复验 && make e2e-bed-down | result: pass | note: p1-1-fix1——up --wait 全 healthy；sshd_config.d 硬化生效；bp1/relay1/relay2 host key 三者互异；权限 700/400 node + diag 读被拒不变；control init=true；down -v 后 0 残留（Makefile 绝对 -f，CWD 无关）
- E2E-11 | stack: docker | command: grep 'FROM debian:12-slim@sha256:' + 文档化 apt 未 pin 范围 | result: pass | note: p1-1-fix1——E2E-11 明确仅覆盖 base digest；apt 未 pin/lockfile-SBOM 作 skeleton 限制记入 Dockerfile 注释（避免 over-claim「可复现」）
- E2E-12 | stack: docker | command: make e2e-bed-up && for c in control bp1 relay1 relay2: ouro version + command -v ouro | result: pass | note: p1-2——四容器 `ouro version` 均 0.1.0、路径均 `/usr/local/bin/ouro`（共享 base 构造一致）；skills/schemas 装于 `/opt/ouro`；远端「sudo ouro tool run 执行目标机本地代码」部分待 p1-3 复验
- E2E-0 | stack: docker | command: make e2e-provision && control ssh -i creds/bp1 ouro-exec@bp1 true | result: pass | note: p1-4——control 经 pubkey SSH `ouro-exec@bp1` 成功、`creds://` 解析、known_hosts 刷新
- E2E-1 | stack: docker | command: control `ouro tool run deploy/preflight --dispatch bp1 --spec /opt/ouro/pool-spec.yaml` | result: pass | note: p1-3/p1-5——派发链路 control→SSH→sudo wrapper→目标 `ouro tool run --machine bp1`→`status:ok` EXIT0；审计在**目标 bp1**（start+finish），control 审计空（目标权威）。真实系统副作用部分待 p1-6
- E2E-1 | stack: docker | command: control `--dispatch bp1 deploy/provision` | result: pass | note: p1-6——派发后目标 bp1 真生成 `/opt/cardano/config/bp1/{config,topology}.json`（属主 node），非 marker；E2E-1 真实系统副作用闭合
- E2E-4 | stack: docker | command: control `--dispatch bp1 deploy/provision` 连跑两次 | result: pass | note: p1-6——第一次 changed=true、第二次 changed=false「already provisioned」，幂等无额外副作用
- E2E-4 | stack: python | command: python3 tests/test_deploy_scripts.py | result: pass | note: p1-6——dev 侧（临时 OURO_CARDANO_ROOT）provision 第二次 changed=false 且断言真生成 config.json/db 目录；S0014 deploy 回归绿
- E2E-2 | stack: docker | command: make e2e-t2 (E2E-2 分项) | result: pass | note: p1-8——ouro-diag `cat`/`tar` 读 `/opt/cardano/keys/kes.skey` 均被拒；目录 `0700` + 文件 `0400` 双断言（TC-14 真容器闭合）
- E2E-3 | stack: docker | command: make e2e-t2 (E2E-3 分项) | result: pass | note: p1-8——ouro-diag 无 sudo；ouro-exec `sudo /usr/local/bin/ouro confirm create` 被拒（仅 wrapper 内 tool run 可行，TC-15 真容器闭合）
- E2E-16 | stack: docker | command: make e2e-t2 (E2E-16 分项) | result: pass | note: p1-8——目标机上以伪造 env + `inv_bogus` token 直调 provision.sh 被 verify-context 拒（TC-4 真容器闭合）
- E2E-15 | stack: docker | command: make e2e-t2 (isolation + teardown) | result: pass | note: p1-7——独立 compose project（`OURO_E2E_PROJECT`）、结束 `down -v` 后 0 残留容器/网络
- （汇总）make e2e-t2 | stack: docker | command: make e2e-t2 | result: pass | note: p1 T2 gate——9 项断言全绿（E2E-15/0/1/4/审计/2/3/16/teardown），即 p3-4 的 `make e2e-t2`
- 安全负向（注入） | stack: docker | command: make e2e-t2（injection 分项） | result: pass | note: p1-fix1——`ouro tool run '<tool>; touch /tmp/pwned #' --dispatch bp1` 被 control `validate_tool_name` 拒且目标机无 `/tmp/pwned`（P0 远端命令注入已修，实测证实）
- TC-13/creds | stack: rust | command: cargo test -q secrets::tests::resolve_rejects_traversal_and_absolute | result: pass | note: p1-fix1——`creds://../../root/.ssh/id_rsa` 等被 `resolve` 拒（路径遍历防护）
- （汇总）make e2e-t2 硬化 | stack: docker | command: make e2e-t2 | result: pass | note: p1-fix1——base 重建（不测陈旧 ouro）、负例验原因、E2E-4 checksum diff、审计 audit_id 关联、relay 派发、E2E-3 scp/rm、E2E-16 任意 audit-id、唯一 project + volume 断言、E2E-12 全绿
- E2E-12 | stack: docker | command: control/bp1/relay 远端 `sudo ouro-tool-run` 执行目标机本地代码 | result: pass | note: p1-3/p1-5——远端 `sudo -n /usr/local/sbin/ouro-tool-run` 在目标机本地执行（非 control），补齐 E2E-12 的远端执行分支

## 7. Change Requests (append-only)
- 2026-07-08T15:40:00+08:00 **Mithril 恢复 = signed Non-goal waiver（p2-6 / E2E-14）**。依据 p2-0 spike（`docs/design/S0015-p2-0-devnet-spike.md`）：private forging devnet 可行，但 **Mithril 需独立 aggregator+signer 栈对某网络出证书**，不能对一次性私有 devnet 运行而不引入大量额外基础设施。故 S0015 将「真 Mithril 恢复」记为 **Non-goal**，以本 waiver 关闭 E2E-14 的「OR signed waiver」分支。
  - 边界（仍受约束，未放弃）：`deploy/sync.sh` 的 **Mithril-mode 证据契约仍强制**——`sync.mode: mithril` 时缺 `OURO_MITHRIL_DIGEST`/`OURO_MITHRIL_CERT_CHAIN` 即 `exit 20 mithril_evidence_missing`（代码在 `ouro-skills/deploy/scripts/sync.sh`，S0014 TC-21 覆盖）；devnet 用 `sync.mode: genesis`（真 genesis 出块已由 p2-0/p2-1 证明）。
  - 复检门：若未来采用支持 Mithril 的 devnet 工具，新开 spec 追加「真 Mithril 恢复 + 证书链证据」项替换本 waiver（不改本行，append-only）。
