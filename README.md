# OuroOps

A macOS menu bar application for managing AWS operations.

## Requirements

- macOS 14.0+
- Xcode 15+

## How to Run

### Using Xcode

1. Open `OuroOps.xcodeproj`
2. Select the OuroOps scheme
3. Press ⌘R to build and run

### Using Command Line

```bash
xcodebuild -project OuroOps.xcodeproj -scheme OuroOps -configuration Debug build
```
