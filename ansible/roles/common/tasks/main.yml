---
- name: Ensure required baseline apt packages are installed
  apt:
    name:
      - chrony
      - curl
      - jq
      - ufw
      - fail2ban
      - python3-docker
    state: present
    update_cache: true

- name: Check whether docker CLI is already available
  command: docker --version
  register: docker_cli_check
  changed_when: false
  failed_when: false

- name: Ensure distro docker package is installed when docker is absent
  apt:
    name:
      - docker.io
    state: present
    update_cache: true
  when: docker_cli_check.rc != 0

- name: Ensure chrony is enabled when configured
  service:
    name: chrony
    state: started
    enabled: true
  when: enable_chrony | default(true) | bool

- name: Create swap file when enabled
  command: "fallocate -l {{ swap_size | default('8G') }} /swapfile"
  args:
    creates: /swapfile
  when: enable_swap | default(true) | bool

- name: Set swap file permissions
  file:
    path: /swapfile
    owner: root
    group: root
    mode: "0600"
  when: enable_swap | default(true) | bool

- name: Check whether swapfile is already active
  shell: "swapon --noheadings --show=NAME | grep -Fx /swapfile"
  register: swapfile_active
  changed_when: false
  failed_when: false
  when: enable_swap | default(true) | bool

- name: Initialize swap space
  command: mkswap /swapfile
  when:
    - enable_swap | default(true) | bool
    - (swapfile_active.rc | default(1)) != 0

- name: Enable swap
  command: swapon /swapfile
  when:
    - enable_swap | default(true) | bool
    - (swapfile_active.rc | default(1)) != 0

- name: Persist swap in fstab
  lineinfile:
    path: /etc/fstab
    line: "/swapfile none swap sw 0 0"
    state: present
  when: enable_swap | default(true) | bool

- name: Configure docker daemon log rotation
  copy:
    dest: /etc/docker/daemon.json
    content: |
      {
        "log-driver": "json-file",
        "log-opts": {
          "max-size": "50m",
          "max-file": "3"
        }
      }
    mode: "0644"

- name: Ensure docker service is restarted after daemon change
  service:
    name: docker
    state: restarted

- name: Ensure docker service is enabled
  service:
    name: docker
    state: started
    enabled: true

- name: Raise file descriptor limit for root
  lineinfile:
    path: /etc/security/limits.conf
    line: "root soft nofile 65535"
    state: present

- name: Raise hard file descriptor limit for root
  lineinfile:
    path: /etc/security/limits.conf
    line: "root hard nofile 65535"
    state: present
